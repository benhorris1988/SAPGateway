name: sap_gateway_server
description: Mock SAP NetWeaver Gateway exposing OData v2 endpoints and a JSON admin API.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.4.0

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  args: ^2.5.0

dev_dependencies:
  lints: ^4.0.0
